const country = 'Germany';
let population = 36000000;
const language = 'German';
const continent = 'Europe';

console.log(
	`${country} has a population of ${population}, and it's located in ${continent}. It's official language is ${language}.`
);
