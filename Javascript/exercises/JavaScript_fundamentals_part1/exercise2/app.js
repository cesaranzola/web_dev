const country = 'Germany'
let population = 34000000;
const isIsland = false;
let language;

console.log(`${country} has a population of ${population} and its language is ${language}. The affirmation that is an Island is ${isIsland}.`);
